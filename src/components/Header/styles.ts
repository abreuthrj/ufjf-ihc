import styled from "@emotion/styled";
import { Box } from "@mui/material";

export const TopBar = styled(Box)((props) => ({
  width: "100%",
  height: 80,
  boxShadow: "-2px 0 6px #00000040",
  marginBottom: 24,
}));
